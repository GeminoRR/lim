func main
	var ballPosition = new vector(10, 2)
	var ballVelocity = new vector(3, -1)

	ballPosition = ballPosition + ballVelocity

	let test = ballPosition
	
	inverseVector(ballPosition)

func inverseVector(vec:vector)
	vec.x = -(vec.x)

primary class vector
	var x:int
	var y:int

	func create(_x:int, _y:int)
		x = _x
		y = _y

	func str
		return "(" + x.str() + ", " + y.str() + ")"

	relation +(vector1:vector, vector2:vector)
		return new vector(vector1.x + vector2.x, vector1.y + vector2.y)