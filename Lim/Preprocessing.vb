'===================================
'========== PREPROCESSING ==========
'===================================
'
' Takes a text and returns that same text without blank lines and comments
'

Public Module Preprocessing

    '=================================
    '========== PRE PROCESS ==========
    '=================================
    Public Function PreProcess(ByVal text As List(Of String)) As List(Of String)

        'Variables
        Dim result As New List(Of String)

        'Return
        Return result

    End Function

End Module
